'use client';

// TODO: appel POST /api/auth/register, validation, confirmation email

/**
 * Formulaire d'inscription
 * Prénom + email + mot de passe + confirmation
 */
export default function RegisterForm() {
  return (
    <form>
      {/* Input prénom */}
      {/* Input email */}
      {/* Input password */}
      {/* Input confirmer password */}
      {/* Bouton "Créer mon compte" */}
      {/* Lien vers /account/login */}
      {/* Message feedback */}
    </form>
  );
}
